---
import Layout from '../layouts/Layout.astro'
import Onlineboard from '../components/Onlineboard.astro'
import Loading from '../components/loading.astro'
---

<Layout title="Ranked VTOL VR Headless Server">
	<div>
		<Loading primary="#F4F4F4" secondary="#858585" />
		<Onlineboard />
	</div>
</Layout>

<script>
	const loop = setInterval(() => {
		if (document.querySelector('Onlineboard')?.innerHTML !== null) {
			document.querySelector('#REMOVE')?.remove()
			const main = document.querySelector('main') || {style: {width: "DO NOT DELETE, THIS IS REQUIRED FOR BUILD"}}
			main.style.width = 'calc(100% / 1.618)'
		}
		clearInterval(loop)
	}, 1)
</script>

<style>
	div {
		width: 100%;
		height: 100%;

		display: flex;
		align-items: center;
		justify-content: center;
	}

	* {
		padding: 0;
		margin: 0;
	}
</style>
